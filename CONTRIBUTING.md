# Contributing

Open an issue first before submitting a PR. This keeps effort aligned and avoids duplicate work.

## Development

```sh
pnpm install
pnpm test          # run tests
pnpm build         # build dist/
pnpm -F playground dev  # run playground
```

## Release

Releases are automated via GitHub Actions on version tags. See [release workflow](.github/workflows/release.yml).
